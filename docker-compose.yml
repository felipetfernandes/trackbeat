services:
  trackbeat:
    container_name: trackbeat

    build:
      context: .
      dockerfile: docker/Dockerfile.dev

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next

    stdin_open: true
    tty: true